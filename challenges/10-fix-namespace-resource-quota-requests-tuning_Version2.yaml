id: 10-fix-namespace-resource-quota-requests-tuning_Version2
title: Namespace hits ResourceQuota; tune requests so all pods can run.
category: quota
namespace: quota-chal
timeLimitSeconds: 900
setup: ../scripts/challenges/setup-quota.sh
grade: ../grading/grade-quota.sh
description: |
  In namespace 'quota-chal', identify pods failing due to ResourceQuota and tune containers' requests (and limits if applicable) so workloads fit within quota. Adjust LimitRange defaults if necessary. Verify all pods can schedule.
