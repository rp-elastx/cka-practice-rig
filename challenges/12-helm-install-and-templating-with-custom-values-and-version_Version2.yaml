id: 12-helm-install-and-templating-with-custom-values-and-version_Version2
title: Install a Helm chart with custom values and a specific version; render templates.
category: helm
namespace: helm-chal
timeLimitSeconds: 900
setup: ../scripts/challenges/setup-helm.sh
grade: ../grading/grade-helm.sh
description: |
  In namespace 'helm-chal', install a specified Helm chart release with a particular version using custom values, then render the manifests locally with the same values without installing. Verify the release resources are present and configured per values.
