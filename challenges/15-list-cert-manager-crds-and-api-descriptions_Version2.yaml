id: 15-list-cert-manager-crds-and-api-descriptions_Version2
title: List CRDs installed by cert-manager and get API descriptions.
category: certmanager
namespace: cert-chal
timeLimitSeconds: 600
setup: ../scripts/challenges/setup-certmanager.sh
grade: ../grading/grade-certmanager.sh
description: |
  List CRDs provided by cert-manager (including acme.*) and obtain API descriptions for key kinds (Certificate, Issuer, ClusterIssuer, CertificateRequest, Challenge, Order). Summarize versions served and essential fields.
