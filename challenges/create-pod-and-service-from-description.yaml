id: create-pod-and-service-from-description
title: Create a Pod and a Service from a textual description.
category: service
namespace: service-chal
timeLimitSeconds: 900
setup: ../scripts/challenges/setup-service.sh
grade: ../grading/grade-service.sh
description: |
  In namespace 'service-chal', create a pod 'echo' exposing HTTP on port 8080 and a ClusterIP service 'echo-svc' that targets the pod. Confirm the service has ready endpoints.
