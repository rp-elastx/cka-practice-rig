id: ingress-config
title: Configure an Ingress to route traffic to a Service.
category: gateway
namespace: gateway-chal
timeLimitSeconds: 900
setup: ../scripts/challenges/setup-gateway.sh
grade: ../grading/grade-gateway.sh
description: |
  In namespace 'gateway-chal', create an Ingress 'app-ingress' routing host 'app.example.com' to service 'svc-name' on port 80 using the installed ingressClass (e.g., nginx). Verify the rule is active.
