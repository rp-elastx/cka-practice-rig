id: modify-configmaps-and-rollout-deployments
title: Modify ConfigMaps and trigger rollouts for Deployments.
category: deployment
namespace: deploy-chal
timeLimitSeconds: 900
setup: ../scripts/challenges/setup-deployment.sh
grade: ../grading/grade-deployment.sh
description: |
  In namespace 'deploy-chal', update ConfigMap 'app-config' and ensure Deployment 'app' picks up the change appropriately. If consumed via env, trigger a rollout; if mounted as files, verify app re-reads updated content. Confirm rollout completes.
