#!/usr/bin/env bash
set -euo pipefail

echo "[setup-static-pod] Static pod challenge ready."
echo "Create a static pod manifest in /etc/kubernetes/manifests/"
echo "Document the path and list all static pods."
